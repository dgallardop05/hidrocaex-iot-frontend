import type { WaterLevelHistory } from '@/types/history.types';
interface Props {
    data: WaterLevelHistory[];
    title: string;
}
declare const WaterLevelChart: ({ data, title, }: Props) => import("react/jsx-runtime").JSX.Element;
export default WaterLevelChart;
